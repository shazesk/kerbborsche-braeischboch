import '@/styles/admin.css';

export const metadata = {
  title: 'Admin Dashboard — Kerbborsche',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
